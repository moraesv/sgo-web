import styled from 'styled-components'

export const H5Style = styled.h5`
  margin: 0;
  font-family: ${({ font }) => font};
  font-size: 14px;
  color: ${({ color }) => color};
  line-height: 18px;
  min-height: 18px;
`
