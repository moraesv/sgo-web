import styled from 'styled-components'

export const H3Style = styled.h3`
  margin: 0;
  font-family: ${({ font }) => font};
  font-size: 24px;
  color: ${({ color }) => color};
  line-height: 28px;
  min-height: 28px;
`
