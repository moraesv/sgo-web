import styled from 'styled-components'

export const H6Style = styled.h6`
  margin: 0;
  font-family: ${({ font }) => font};
  font-size: 12px;
  color: ${({ color }) => color};
  line-height: 16px;
  min-height: 16px;
`
